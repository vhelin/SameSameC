
  .SECTION "main" FREE
    ; =================================================================
    ; test2.blb:3: void main() {
    ; =================================================================
    main:
      ; A  - tmp
      ; BC - tmp
      ; DE - frame pointer
      ; HL - tmp
      ; SP - stack pointer
      ; IX - tmp
      ; IY - tmp
      ; =================================================================
      ; test2.blb:5: while (1) {
      ; =================================================================
      ; -----------------------------------------------------------------
      ; TAC: label_1:
      ; -----------------------------------------------------------------
    label_1:
      ; -----------------------------------------------------------------
      ; TAC: if 1.uint8 (uint8) == 0.uint8 (uint8) jmp label_2
      ; -----------------------------------------------------------------
      LD  A,1
      LD  B,0
      SUB A,B
      JP  Z,label_2
      ; -----------------------------------------------------------------
      ; TAC: jmp label_1
      ; -----------------------------------------------------------------
      JP  label_1
      ; -----------------------------------------------------------------
      ; TAC: label_2:
      ; -----------------------------------------------------------------
    label_2:
      ; -----------------------------------------------------------------
      ; TAC: return
      ; -----------------------------------------------------------------
      LD  IX,0
      ADD IX,DE
      LD  L,(IX+0)
      LD  H,(IX-1)
      JP  (HL)
  .ENDS

